# H15.2 · Tragaderos: ¿llega la CAUSA al log y algo útil a la pantalla?

Árbol: `tyrian-companion` worktree `agent-a25e5f8071b0bd68b`, HEAD `a756cda7b976` = `origin/main` (medido con `git rev-parse HEAD` / `git rev-parse origin/main`). Censo: `node scripts/action-observability-census.mjs` → PASS, 693 fronteras en 231 ficheros (`scripts/action-observability-baseline.json`); en alcance 675 (fuera: `src/platform/` = 18).

Mecánica que decide casi todos los veredictos (verificada en `src/core/local-debug-action-runner.ts`):
- `LocalDebugActionRunner.run()` escribe `failure` con `message: error` SOLO si la acción LANZA (`writeFailure`, l.170-183). Si la acción DEVUELVE un resultado clasificado (`{status:'failed'}`, `'unavailable'`, `AlertDeliveryReport`…), `writeOutcome` (l.150-168) solo lo reconoce como fallo si el valor tiene la forma `{phase|code|state|details}` (`isOutcome`, l.334-341); si no, escribe `info success ok`.
- `LocalDebugPersistenceProbe.failure(code)` (`src/core/local-debug-persistence.ts` l.107-125) transporta SOLO un código; el `error` capturado nunca entra en el registro (`details: {store, operation}`, l.143-157). `localDebugStorageFailureCode` (l.161) distingue cuota, pero solo lo usan `price-history-store` y `halloween-store`; los demás 8 stores llaman `attempt.failure()` pelado = `storage_failure` siempre.
- `emitNotice` (`src/main.ts` l.2360-2367) registra `notification_emit` con `state: source`, nunca el motivo del aviso.

Columna «n» = fronteras del censo que cubre la fila (una fila = un punto de fallo o una familia con veredicto idéntico; la línea citada es la del censo).

## 1. Tabla

| n | fichero:línea | construcción | qué error cae aquí | registro en log con causa | mensaje al usuario | veredicto |
|---|---|---|---|---|---|---|
| 1 | `src/ui/session-command-controller.ts:61` | `.catch` | CUALQUIER excepción de start/finish/recover/discard/clear (`execute()` = `startManualSession`, `performStopManualSession`, `performRecoverSession`, `performDiscardRecoveredSession`, `performClearCompletedSession`) | NO. El catch no liga el error, devuelve `'failed'`; `run()` resuelve `void`, así que los `run({session_start\|session_finish\|session_recover\|session_discard\|session_clear})` de `main.ts` l.2532, 2555, 2586, 2591, 2596, 2603, 2801, 3261 escriben `info success ok`. 10 sep: 0 líneas | Genérico: `commands.actionFailed` («No se pudo completar la acción de sesión») | **TRAGADERO** (raíz del incidente del 10 sep; en curso H15.1 para start/stop, NO para recover/discard/clear) |
| 4 | `src/ui/session-command-controller.ts:45,65,68` (+ rama `'unavailable'` l.47-58) | callback / `void` / rama | Comando que deja de estar disponible entre intención y ejecución | NO (ningún puerto de diagnóstico en el controlador) | `commands.actionUnavailable` | TRAGADERO (leve: sin causa de por qué dejó de estar disponible) |
| 1 | `src/sessions/manual-session-start-service.ts:745` | `catch (error)` → `mapFailure` (l.1265) | Cualquier fallo de captura de baseline, lease, persistencia; todo lo no tipado → `'unexpected'` | NO: `mapFailure` descarta el error; `failWithoutLease`/`cleanupFailedStart` (l.1170, 1131) no llaman a `this.diagnostics`, que solo se usa en `logRecoveryBusy` (l.1214) | `status.startFailure.<code>` (útil solo para códigos tipados; `unexpected` no dice nada) | **TRAGADERO** (en curso H15.1) |
| 1 | `src/sessions/manual-session-start-service.ts:696` | `catch` → `mapFailure` | `normalizeSessionStartInput` lanza (entrada inválida) | NO | `status.startFailure.invalid_input` | TRAGADERO (leve: validación) |
| 1 | `src/sessions/manual-session-start-service.ts:840` | `catch (error)` → `mapStopFailure` (l.1278) | Fallo del snapshot final, `compareStorageSnapshots`, persistencia; todo lo no tipado → `'snapshot_failed'` con texto «puedes reintentar» | NO (mismo patrón); `failStop` (l.1163) no registra | `status.stopFailure.<code>` | **TRAGADERO** (en curso H15.1) |
| 6 | `src/sessions/manual-session-start-service.ts:755-780,792` (ramas `failStop('unexpected'…)`, `'coordination_unavailable'`, `'delta_invalid'`), fuera del censo | rama | Estado inconsistente al parar (sin sesión activa, sin baseline, transición rechazada, ventana inusable, delta inválido) | NO | `status.stopFailure.unexpected` | TRAGADERO (en curso H15.1) |
| 5 | `src/sessions/manual-session-start-service.ts:686-714` (ramas `failWithoutLease`), fuera del censo | rama | Servicio dispuesto, recuperación pendiente, sesión ya activa, lease ocupado, coordinación caída | NO | `status.startFailure.busy` / `coordination_unavailable` (útil) | TRAGADERO (leve: el mensaje sí es útil, pero no queda traza) |
| 1 | `src/sessions/manual-session-start-service.ts:806` | `catch` | `priceCapture.capture` lanza al parar | NO: se sustituye por `unavailableSessionPriceSnapshot` sin registrar | Nota de sesión sin valoración («precio no disponible») | TRAGADERO (medio) |
| 3 | `src/sessions/manual-session-start-service.ts:1201,1231,1235` | `catch` → `{status:'error', code:'unavailable'}` | `coordinator.acquire/assertOwned/release` lanzan | NO aquí; `coordination-store` registra `coordination write failure storage_failure` sin el error (ver familia stores) | `coordination_unavailable` | TRAGADERO (parcial) |
| 2 | `src/sessions/manual-session-start-service.ts:1057,1084` | `catch` | Heartbeat / `failFromAuthority` fallan al persistir | NO (`persistCurrentState().catch(() => undefined)` l.1083) | Ninguno; la sesión pasa a `error` por `authorityFailure` | TRAGADERO (medio) |
| 2 | `src/sessions/manual-session-start-service.ts:1143,1152` | `catch` | Transición `fail`/`reset` rechazada en `cleanupFailedStart` | NO | Ninguno | TRAGADERO (leve) |
| 15 | `src/sessions/manual-session-start-service.ts:238,302,319,360,376,534,1030,1035,1083(×2),347,408,956,1188` | `void` / callback / fallback | `void this.stop()` (settlement), flights, `getSettlementWait`, dispose, clock | `stop()` cae en la fila 840; el resto inerte | — | OK (inerte) / cubierto por fila 840 |
| 1 | `src/main.ts:2681` (`throw new Error('Stop failed.')` en `performStopManualSession`) + `:2682` catch rethrow | rama + `catch` | `sessions.stop()` devuelve `{status:'failed', failure}` | PARCIAL: cuando el camino es `captureSessionFinalNow`/`onSettlementDue` (`run(session_finish)` l.2614, `fireAndForgetLocal` l.898) llega `failure unknown_failure message:"Stop failed."` SIN `failure.code` ni `failure.message`; cuando el camino es el botón/comando lo traga la fila 1 | `status.stopFailure.<code>` | **TRAGADERO** (en curso H15.1) |
| 1 | `src/main.ts:2873` (`throw new Error('Start failed.')`) + `:2874` catch rethrow | rama + `catch` | `sessions.start()` devuelve `failed` | PARCIAL solo por `pending_start`/`pilot_boundary` (l.1755-1767, 2813-2817: `fireAndForgetLocal(session_start)` recibe «Start failed.» sin código); por botón/comando lo traga la fila 1 | `status.startFailure.<code>` + `notices.pendingStartFailed` | **TRAGADERO** (en curso H15.1) |
| 1 | `src/main.ts:2870` | `catch` | `updateSettings({preferredCharacter})` falla tras arrancar | `updateSettings` va por `run(settings_save)` l.3025 → SÍ con causa | Ninguno (correcto: la sesión no depende de ello) | OK |
| 2 | `src/main.ts:3329,3349` (+ `throw 'Recovery failed.'` l.3336, `'Discard failed.'` l.3356) | `catch` rethrow + rama | `sessions.recover()/discardRecovery()` devuelven `{status:'busy'\|'failed', message}` | NO: el `message` del servicio se descarta al lanzar `Error('Recovery failed.')`, y ese Error lo traga la fila 1. Solo sobrevive `logRecoveryBusy` (l.1214) para el caso lease ajeno | `commands.actionFailed` + `recoveryState.message` en la tarjeta | **TRAGADERO** (no está en H15.1) |
| 10 | `src/main.ts:3325,3330,3335,3340,3345,3350,3355,3358,1736,2675` | `void this.pilotMetrics.*` | `recoveryFinished`, `proposalDecided`, `ensurePilotRecoveryPresented` rechazan | NO: `pilot-metrics-recorder.ts` traga todo (ver familia) | Ninguno | TRAGADERO (leve, telemetría del piloto) |
| 3 | `src/ui/product-action-controller.ts:283` (+234 callback) | `void …run(id).catch(() => undefined)` | Paleta de comandos: cualquier acción de producto que lance o devuelva `'failed'` (l.207 la convierte en `Error('Product action failed.')`) | NO en este punto. Para comandos de sesión ya lo tragó la fila 1; para el resto el `execute` de `main.ts` (l.3208-3236) va por `run()` y sí registra lo que LANZA, pero no lo que devuelve `'failed'` (`advisorActionOutcome`/`vaultSyncActionOutcome`/`detectionActionOutcome` l.3651-3706) | Feedback «error» genérico en la barra (`productAction.feedback.error`) | **TRAGADERO** |
| 8 | `src/ui/product-shell.ts:44,45,171` (+5 callbacks) | `void run().catch(() => undefined)` | Botones de la barra de acciones / nav | Igual que la fila anterior | Igual | TRAGADERO (mismo punto de fallo que 283) |
| 1 | `src/ui/product-action-controller.ts:212` | `catch` rethrow | — | Relanza; la causa se pierde aguas abajo (283 / 171) | «error» | cubierto arriba |
| 1 | `src/ui/session-command-adapter.ts:57` | `void controller.run(id)` | — | `run()` nunca rechaza (fila 1) | — | cubierto por fila 1 (`registerSessionPalette` no se usa: `grep registerSessionPalette src/main.ts` = 0) |
| 1 | `src/main.ts:1709` | `catch` (sin ligar) | `pendingProposals.acknowledge` o `activateView` lanzan al revisar una propuesta | NO: `run(detection_proposal review)` ve `return 'failed'` → `success ok`; `emitNotice` solo escribe `notification_emit state:proposal_review_failed` | `notices.proposalReviewFailed` (genérico) | **TRAGADERO** |
| 1 | `src/main.ts:1704` (rama `'unavailable'`), fuera del censo | rama | `acknowledge()` devuelve false (`pending-proposal-service` tragó la causa, ver fila) | NO | `notices.proposalUnavailable` | TRAGADERO (medio) |
| 1 | `src/main.ts:931` (rama `{status:'unavailable'}` en `onProposal`), fuera del censo | rama | Propuesta de parada sin sesión activa; `enqueue` devolvió `unavailable` | NO; `assisted-detection-service.ts:427` traga el `false` («remain paused») | Detector queda en `*_proposed` sin explicar por qué no pasó a la cola | TRAGADERO (medio) |
| 8 | `src/sessions/pending-proposal-service.ts:101,105,124,161,188,208,247,274` | `catch` → `unavailable()` | Cualquier excepción del store de propuestas o de la proyección | PARCIAL: `pending-proposal-store` (l.58, 93, 131) emite `detection_proposal failure storage_failure` SIN el error; la excepción de proyección (l.105/247) no se registra en absoluto | Cola «no disponible» (`view.pendingUnavailable`) | TRAGADERO (parcial) |
| 1 | `src/sessions/pending-proposal-service.ts:69` | callback | — | — | — | OK (inerte) |
| 1 | `src/sessions/assisted-detection-service.ts:323` | `catch (error)` → `fail(msg)` | Captura del snapshot inicial al armar lanza (lo no-429 → «could not be captured») | PARCIAL: `http_request failure` con `statusCode` SÍ (http.ts l.179-200), pero `run(detection_arm)` de `main.ts` l.1800 recibe `'failed'` → `success ok`; el error no-HTTP (parseo, invariante) se pierde entero | «Error · detección detenida» + `message` fijo | **TRAGADERO** (medio) |
| 4 | `src/sessions/assisted-detection-service.ts:412,422,427` (+1 callback) | `void` + `catch` | `onProposal` lanza o devuelve false | NO (comentario «remain paused») | El detector se queda en `*_proposed` | TRAGADERO (medio) |
| 1 | `src/sessions/api-poll-scheduler.ts:231` | `.catch` → `publish('fatal')` | `onTimer` rechaza (poll lanzó) | SÍ: l.285 `finishDiagnostic(... 'failure', schedulerFailureCode(error), …, error)`; 296 captura `pollError` | Detector `error`/scheduler `fatal` | OK |
| 8 | `src/sessions/api-poll-scheduler.ts:285,296,365,395,426,442` (+2 callbacks) | `catch` | diagnóstico fail-open | SÍ (285, 426 registran con `message: error`) | — | OK |
| 12 | `src/sessions/coordination-coordinator.ts:83,107,125,144,151,194,196,216,239` (+3 callbacks) | `catch` → `{status:'error', code:'unavailable'\|'clock_anomaly'}` | Transacción IndexedDB del lease falla, reloj retrocede, `sleep` falla | PARCIAL: `coordination-store.ts` (l.54, 68, 75-76, 90, 108-109) emite `session_lease failure storage_failure` sin el `error`/`DOMException.name`; `clock_anomaly` no se registra | `coordination_unavailable` | TRAGADERO (parcial) |
| 4 | `src/sessions/coordination-store.ts:*` (4 fronteras del censo) | callbacks IDB / `catch` | `onerror`/`onabort` | PARCIAL (código sin causa) | — | TRAGADERO (parcial, familia stores) |
| 8 | `src/sessions/session-runtime-store.ts:*` (l.154,179,201,217,256,270,292,306) | `catch` → `attempt.failure()` | Leer/guardar/limpiar el registro de sesión falla (cuota, `InvalidStateError`, DB cerrada) | PARCIAL: `session_lease failure storage_failure` SIN `errorName`; `localDebugStorageFailureCode` NO se usa aquí (`grep` = solo price-history y halloween) | `recoveryState error code:'unavailable'` («no se pudo leer la sesión guardada») | TRAGADERO (parcial: es EL store de la sesión) |
| 4 | `src/sessions/pending-proposal-store.ts:*` | `catch (error)` → `attempt.failure()` | idem | PARCIAL (`error` ligado y no usado, l.58/93/131) | — | TRAGADERO (parcial) |
| 5 | `src/sessions/session-detection-quality-store.ts:*` | `catch` → `attempt.failure()` | idem | PARCIAL | Fila «calidad de detección no disponible» | TRAGADERO (parcial) |
| 3 | `src/sessions/session-detection-quality-recorder.ts:86,115` + 1 | `catch` → estado `unavailable` | `store.load/append` lanzan | PARCIAL (solo lo que dijo el store) | idem | TRAGADERO (parcial) |
| 2 | `src/sessions/loot-presentation-cache.ts:58,69` | `catch (cause)` → `{status:'failed', stage, code, cause}` | Proyección de loot falla | SÍ: devuelve `cause`; `refreshLootPresentation` (main.ts l.3479-) abre span. **Verificado que el `cause` llega al span**: no; solo `code`/`stage` (ver `refreshLootPresentation`) — parcial | Resumen «no disponible» | TRAGADERO (parcial, leve) |
| 14 | `src/sessions/session-history.ts:192,206,217,228,252,286,305,323,357,374,390,520` +2 callbacks | `catch` → `'conflict'`/`'unavailable'` con mensaje fijo | `vault.read/create/process/createFolder` lanzan (permisos, fichero bloqueado, Sync) | NO: la clase no tiene puerto de diagnóstico (`grep diagnostics` = 0) y `main.ts` no envuelve `exportSessionHistory` (l.1977) ni `loadSessionHistory` (l.1970) en `run()`; `previewSessionHistoryScrub`/`scrubSessionHistory` (l.1995/2035) tienen `.catch` → `'unavailable'` sin registrar | Fila de ajustes «no disponible» / «conflicto» (genérico) | **TRAGADERO** |
| 1 | `src/main.ts:1982` | `catch` | `sessionHistory.export` lanza | NO | idem | TRAGADERO |
| 6 | `src/main.ts:1995,2035` (2 callbacks + `.catch` ×2) | `.catch` → `'unavailable'` | preview/scrub lanzan | NO | idem | TRAGADERO |
| 1 | `src/main.ts:2745` | `catch` → `{status:'unavailable'}` | `sessionHistory.readSession` o `sha256Text` lanzan al comprobar si la nota existe | NO; `sessionSummarySaveState='failed'` sin traza | Tarjeta «resumen no guardado» + botón reintentar | **TRAGADERO** |
| 5 | `src/sessions/session-note-writer.ts:37,56,63,80,117` | `catch` → `{status:'unavailable', message fijo}` | `vault.create/read/process/createFolder` lanzan al escribir LA nota de sesión (única entrega del resumen) | NO: sin diagnóstico en el writer; `persistCompletedSessionSummary` (`main.ts` l.2773) tampoco registra `note.status`/`message`: solo `emitNotice(…,'session_command')` | `notices.sessionSummaryNotSaved` (genérico) | **TRAGADERO** |
| 1 | `src/main.ts:2773` | `catch` (sin ligar) | `sessionNotes.write` lanza | NO | idem | **TRAGADERO** |
| 3 | `src/main.ts:2719` (`void pilotMetrics.sessionCompleted`), ramas `runtime === null` l.2721/2755 | `void` / rama | — | NO | `notices.sessionSummaryNotSaved` | TRAGADERO (leve) |
| 1 | `src/main.ts:1761` | `catch (error)` → notice + rethrow | `startManualSession` lanza desde la propuesta pendiente | PARCIAL: `fireAndForgetLocal(session_start)` registra «Start failed.» sin código (fila 2873) | `notices.pendingStartFailed` | TRAGADERO (en curso H15.1) |
| 2 | `src/main.ts:1829,1830` | `.catch(() => false)` + `catch` | `pilotMetrics.proposalExcluded` | NO | Ninguno | TRAGADERO (leve) |
| 4 | `src/main.ts:1687,1839,905,888` | `void pilotMetrics.proposalPresented/proposalExcluded`, `void ensurePilotRecoveryPresented().then()` | Rechazo → sin `.catch` en 888/953 → `unhandledrejection` | 888/953: SÍ pero como `global_error unattributed_origin` (l.416-424), sin acción; 905/1687/1839: NO (recorder traga) | Ninguno | TRAGADERO (leve) |
| 1 | `src/main.ts:953` | `void ensurePilotRecoveryPresented(...).then(renderViews)` | idem | idem | — | TRAGADERO (leve) |
| 1 | `src/main.ts:1089` | `void this.armAssistedDetection()` | `arm` lanza | SÍ por `run(detection_arm)` (l.1800) + `unhandledrejection` duplicado | Detector `error` | OK (log) |
| 1 | `src/main.ts:1819` | `void this.excludeLiveAssistedProposal()` | — | NO | — | TRAGADERO (leve) |
| 1 | `src/main.ts:2106` | `catch` vacío | `sellSignal.ensureSeed()`/`evaluate` lanzan dentro de la compactación | NO (comentario «never fails a compaction»); la señal de venta deja de evaluarse en silencio | Ninguno: no hay alerta y nada lo dice | **TRAGADERO** (medio) |
| 1 | `src/main.ts:2270` (`.catch(() => null)` en `ensureAlertIngameServer`) | `.catch` | `EADDRINUSE`/`EACCES` al abrir el puerto loopback | PARCIAL: `syncAlertIngameServer` (l.2257) lanza `Error('…could not be started.')` → `notification_emit failure` con ese texto, SIN `error.code` del socket; `alert-ingame-server.ts:82` relanza bien pero 2270 lo tira | Ninguno en ajustes (la fila del puerto no cambia) | **TRAGADERO** (medio) |
| 7 | `src/main.ts:2250,2267,2270(×2 callbacks),2289(×2)` | `void stale.close()`, `.then(…, () => null)` | cierre / sha256 | NO | — | TRAGADERO (leve) |
| 2 | `src/alerts/alert-emitter.ts:61` (+ `.then(() => true, () => false)` l.65) | `catch` → `false` | Cada canal (`system_notification`, `sound`, `webhook`, `ingame`, `queue`) lanza con un mensaje concreto (`main.ts` l.2175, 2181, 2196, 2210-2211, 2218) | NO: el mensaje se convierte en `false`; `emit()` devuelve `{delivered, failed}` que `fireAndForgetLocal(notification_emit)` (l.2092-2095) registra como `success ok` (no es `isOutcome`) | Solo el toast; el jugador no sabe que el webhook/in-game/cola fallaron | **TRAGADERO** (medio-grave: la alerta es el producto) |
| 2 | `src/alerts/alert-webhook.ts:147` (+1 callback) | `catch` → `'failed'` | Transporte del webhook lanza / timeout | PARCIAL: `http_request failure` de `ObsidianRequestTransport` con `statusCode` SÍ; el timeout propio (`deadline`) no | Ninguno | TRAGADERO (parcial) |
| 3 | `src/alerts/alert-system-notification.ts:59`, `alert-sound.ts:100`, `alert-webhook.ts:105` | `catch` → `'unavailable'`/`null` | Notification API / AudioContext lanzan; URL inválida | NO | Ninguno | TRAGADERO (leve) |
| 6 | `src/alerts/emitted-alert-queue.ts:44,52,61,69` +2 | `catch` → `false`/`[]` | IndexedDB de la cola de alertas falla | NO (sin probe: `grep diagnostics` = 0) → el canal `queue` falla y la fila anterior lo traga | Panel de alertas vacío | TRAGADERO (medio) |
| 4 | `src/alerts/alert-ingame-server.ts:82` +3 callbacks | `catch` rethrow | — | relanza (bien) | — | OK aquí; lo tira `main.ts:2270` |
| 1 | `src/alerts/alert-queue-record.ts:85` | `catch` → null | fecha inválida | — | — | OK (inerte) |
| 1 | `src/account/connection-service.ts:71` | `catch (error)` → `failedState` | `checkConnection` lanza | PARCIAL: HTTP → `http_request failure statusCode`; no-HTTP (`parseAccountProfile`, esquema) → `ConnectionCheckError('unavailable')` sin causa; `run(connection_check)` (`main.ts` l.1098) devuelve el estado → `success ok` aunque sea `error` | Motivo tipado en la tarjeta (`key_invalid`, `unavailable`…) = útil | TRAGADERO (parcial: no hay línea `connection_check failure`) |
| 3 | `src/account/account-service.ts:85,94,103` | `catch` rethrow `mapConnectionError` | idem | idem | idem | cubierto arriba |
| 1 | `src/account/rate-limited-storage-snapshot-service.ts:55` | `catch` rethrow | 429 | SÍ (http + `rate_limit_coordinator`) | — | OK |
| 1 | `src/account/guild-wars-2-client.ts:82` | `catch` rethrow | — | SÍ (http) | — | OK |
| 4 | `src/account/storage-snapshot-service.ts:478,600` (+2 callbacks) | `catch` | anchor recheck / fuente opcional | 600 relanza no-HTTP; 478 `false` sin registro (solo afecta a `quality`) | — | OK / leve |
| 3 | `src/account/trading-post-evidence.ts:113,127,175` | `catch` → `historyFailure('unavailable')`/`request_failed` | Evidencia de TP falla | PARCIAL (http sí; motivo agregado no) | Nota: «evidencia TP no disponible» | TRAGADERO (parcial, leve) |
| 4 | `src/account/storage-delta.ts:113,263`, `contamination.ts:118,135` | `catch` → `invalid` con `reasons` | Invariante del delta/contaminación | La razón viaja en el resultado; nadie la registra (`manual-session-start-service` l.792 → `failStop('delta_invalid')`, fila 840) | `stopFailure.delta_invalid` | TRAGADERO (parcial, vía fila 840) |
| 1 | `src/core/http.ts:287` | `catch (error)` → `HttpTransportError('network', …, 'Network request failed.')` | `requestUrl` lanza (`net::ERR_NAME_NOT_RESOLVED`, TLS, JSON inválido) | PARCIAL: l.179-200 registra `failure network_failure` con `message` = «Network request failed.» — el mensaje ORIGINAL de Electron se pierde aquí | Depende del caller (`unavailable`) | TRAGADERO (parcial: único sitio que podría decir POR QUÉ no hay red) |
| 4 | `src/core/http.ts:179,206-221,234,264` | `catch` | diagnóstico fail-open | SÍ | — | OK |
| 1 | `src/core/rate-limit-coordinator.ts:77` | `catch` | fail-open | — | — | OK |
| 1 | `src/core/settings.ts:319` | `catch` → default | URL de webhook inválida | NO | Ninguno: se silencia la URL que escribió el usuario | TRAGADERO (leve) |
| 1 | `src/main.ts:362` | `catch (error)` | `loadSettings` lanza | SÍ (l.369-373 `settings_load failure storage_failure message`) + rethrow → Obsidian «Failed to load plugin» | Obsidian | OK |
| 4 | `src/main.ts:404,416,427,440` | callbacks `window error/unhandledrejection/online/offline` | — | SÍ (`global_error`, pero `state:'unattributed_origin'`) | — | OK (es la red de seguridad; no atribuye) |
| 2 | `src/main.ts:453,464` | `visibilitychange`, `onLayoutReady` → `fireAndForget(initializeRuntime)` | `initializeRuntime` lanza | SÍ (`plugin_load runtime_initialize failure` con causa) | NINGUNO: `runtimeReady` queda false y cada botón dice «el plugin sigue arrancando» (`notifyRuntimeStarting`, 28 sitios) para siempre | **TRAGADERO** (UI: el usuario no distingue «arrancando» de «arranque roto») |
| 28 | `src/main.ts` ramas `if (!this.runtimeReady) { notifyRuntimeStarting(); return … }` (l.1697, 1782, 1990, 2020, 2386, 2404, 2428, 2442, …), fuera del censo | rama | Runtime no listo (por arranque en curso o roto) | NO (solo `notification_emit state:plugin_starting`) | «Tyrian Companion sigue arrancando» | cubierto por la fila anterior |
| 1 | `src/main.ts:999` | `.catch(() => undefined)` en `onunload` | `shutdownRuntime` lanza | `run(plugin_unload)` l.1057 registra con causa antes | — | OK |
| 3 | `src/main.ts:1046,1057-1069` | `void pilotProposalClosure.finally` | — | — | — | OK (unload) |
| 1 | `src/main.ts:1376` | `catch (error)` → `span.failure` | catálogo de precios | SÍ | — | OK |
| 1 | `src/main.ts:3473` | `catch (error)` → `span.failure` | catálogo de sesión | SÍ | — | OK |
| 1 | `src/main.ts:1940` | `void openLinkText` | Obsidian no abre la nota | NO | Ninguno | TRAGADERO (leve) |
| 2 | `src/main.ts:1943` (`retrySessionSummarySave`) y `:2534` (`rotateToNewSession`), fuera del censo: sin `run()` | método público invocado con `void` desde `companion-view.ts:640,643` | Lanzan | Solo por `unhandledrejection` → `global_error unattributed_origin` | Nada o `notices.newSessionBlocked` | TRAGADERO (medio) |
| 1 | `src/main.ts:2400` | `catch` | `managedAssets.preview` lanza | NO: `previewManagedAssets` no está en `run()` ni pasa por `managed-assets-lifecycle` (que sí abre span) | Fila de ajustes `inspect_failed` | **TRAGADERO** |
| 1 | `src/main.ts:2509` | `catch` → `'unavailable'` | `managedAssetsLifecycle.install/move/remove` lanzan | PARCIAL: el lifecycle abre span (`managed-assets-lifecycle.ts` l.20-30, 113-122, 156-165) y registra `failure` con el error; pero lo que llega aquí ya es una excepción re-lanzada tras registrarla → OK en log; en UI `managedAssetsFailureCode('unavailable')` | Fila de ajustes | OK (log) / genérico (UI) |
| 3 | `src/assets/managed-assets.ts:202,314,523` | `catch` → `{status:'unavailable', message fijo}` | `vault.create/read/modify` lanzan durante apply/relocate/uninstall | PARCIAL: el lifecycle registra `span.failure(new Error('managed_assets_unavailable'), …, {message})` con el texto FIJO; el error del vault se perdió aquí | Fila de ajustes | TRAGADERO (parcial) |
| 8 | `src/assets/managed-assets.ts:131,139,147,342,376,550,705,742` | callbacks / `catch` | carreras de create, manifest corrupto, YAML | 550 → `'conflict'` sin causa; resto inerte/relanza | «conflicto» | TRAGADERO (leve) para 550; OK resto |
| 7 | `src/assets/managed-assets-lifecycle.ts:*` | `catch (error)` → `span.failure` | — | SÍ | — | OK |
| 6 | `src/assets/managed-assets-pointer.ts:*` | `catch (error)` → `attempt.failure('storage_failure')` | IDB del puntero | PARCIAL (código, sin `error`) | — | TRAGADERO (parcial, familia stores) |
| 3 | `src/ui/inventory-vault-sync-run-controller.ts:140,146,161` | `catch` (sin ligar) → `settle('error','capture_unavailable'\|'unexpected_failure')` | `previewSync()`/`applyPlan()` lanzan (escritura de notas de inventario) | NO: sin diagnóstico en el controlador; `run(inventory_sync)` (`main.ts` l.1444/1450) ve el estado devuelto → `success ok`. Solo la fase `refreshAdvisor` deja traza (workflow span) | Estado «error inesperado» en el panel | **TRAGADERO** |
| 2 | `src/ui/vault-sync-controller.ts:92,118` | `catch` → `'capture_unavailable'`/`'unexpected_failure'` | preview/apply de inventario o cartera lanzan | NO (idem) | «error» genérico | **TRAGADERO** |
| 2 | `src/inventory/inventory-vault-sync.ts:420,458` | `catch` → `'conflict'`/`'unavailable'` | `vault.create/process` lanzan | NO: nadie registra el `message`; `applyInventoryVaultSync` (`main.ts` l.1478) → `success ok` | `vaultSyncActionOutcome` → «failed» | **TRAGADERO** |
| 3 | `src/inventory/inventory-vault-sync.ts:590,693` +1 | `catch` | frontmatter / carpeta | 693 relanza; 590 `'conflict'` sin causa | — | leve |
| 2 | `src/wallet/wallet-vault-sync.ts:239,264` | `catch` → `'conflict'`/`'unavailable'` | idem cartera | NO; `emitNotice(walletVaultSyncNoticeText)` registra `notification_emit state:wallet_sync` sin causa | Notice con el `message` fijo (útil a medias) | **TRAGADERO** |
| 3 | `src/wallet/wallet-vault-sync.ts:352,414` +1 | `catch` | frontmatter / carpeta | — | — | leve |
| 4 | `src/ui/inventory-advisor-controller.ts:164` (+3 callbacks) | `.catch` → `failed = true` | `ports.load` lanza | SÍ: `inventory-advisor-workflow.ts:103/158` `span.failure(error)` antes; y `assemble-advisor.ts:182` escribe recibo | «no se pudo cargar» | OK (log) |
| 2 | `src/advisor/inventory-advisor-workflow.ts:103,158` | `catch (error)` → `span.failure` | — | SÍ | — | OK |
| 2 | `src/runtime/assemble-advisor.ts:131,182` | `catch` | recibo | SÍ | — | OK |
| 2 | `src/advisor/inventory-advisor-classifier.ts:67,98` | `catch` → `invalid()` | BUG del clasificador (excepción en la lógica) | PARCIAL: el workflow registra `inventory_advisor_validation_failed` sintético, sin stack del bug | «bloqueado» | TRAGADERO (medio: un bug propio se disfraza de datos inválidos) |
| 4 | `src/advisor/inventory-advisor-classifier.ts:121,136,734,756` | `catch` → false | validadores | — | — | OK (inerte) |
| 10 | `src/advisor/inventory-advisor-evidence.ts:118,188,202,210,309,348,396,451,475` +1 | `catch` → cobertura `unavailable`/`request_failed` | Captura de evidencia falla por endpoint | PARCIAL: http sí; 348 (`parseAccountProfile`) y 396/451/475 no-HTTP se pierden; 202 traga el fallo del recibo | Advisor «limitado»/«bloqueado» con motivo tipado | TRAGADERO (parcial, leve) |
| 22 | `src/advisor/inventory-advisor-discard.ts:*` (×4), `-contract.ts:*` (×4), `-presentation.ts` (×2), `-result.ts` (×3), `-evidence-contract.ts` (×2), `-builtin-bundle.ts` (×2), `-container-economy.ts` (×3), `-preferences-contract.ts` (×2) = 22 | `catch` → false/null/invalid | validadores puros | — | — | OK (inerte) |
| 11 | `src/advisor/inventory-preferences-runtime.ts:*` | `catch (error)` → `span.failure` | — | SÍ | — | OK |
| 5 | `src/advisor/inventory-preferences-store.ts:*` | `attempt.failure(preferenceFailureCode(code))` | IDB | PARCIAL (código sí, cuota sí) | — | OK-ish (mejor que el resto de stores) |
| 1 | `src/advisor/inventory-preferences-service.ts:168` | `catch` → false | — | NO | — | leve |
| 10 | `src/catalog/persistent-catalog-cache.ts:*` | `catch (error)` → `attempt.failure()` | IDB del catálogo | PARCIAL (código) | — | TRAGADERO (parcial, familia stores) |
| 3 | `src/catalog/public-catalog-service.ts:233,285` +1 | `catch` | 404 / entrada malformada | 233 http sí; 285 `malformed` no se registra | Ítem sin nombre | leve |
| 21 | `src/economy/price-history-store.ts:*` | `attempt.failure(localDebugStorageFailureCode(error))` | IDB | PARCIAL (código + cuota) | — | OK-ish |
| 12 | `src/economy/price-history-runtime.ts:*` | `catch (error)` → `span.failure` / `storeFailure` | — | SÍ salvo 171/274 (`storeFailure` no registra; el span de `activate` l.106 se cierra con `finishPriceHistorySpan(status)` sin el error) | Estado `store_unavailable` | OK / parcial (171, 274) |
| 3 | `src/economy/price-history-capture.ts:73,113` +1 | `catch` → `'invalid_payload'\|'store_unavailable'` | parseo / store | PARCIAL: runtime l.304-306 registra `Error('price_history_invalid_payload')` sintético | — | TRAGADERO (parcial) |
| 3 | `src/economy/price-seed-panel-service.ts:104,142,168` | `catch` | IDB semilla | 104/142 SÍ; 168 (`open`) NO | Bloque de nota «no_seed unreachable» (`price-history-note-block-controller.ts:67`) | leve |
| 3 | `src/economy/price-seed-cache-store.ts:*` | `catch` → reject | — | relanza | — | OK |
| 2 | `src/economy/price-seed-source.ts:79,93` | `catch` → `no_seed` | transporte | http sí | «sin semilla» | OK |
| 20 | `src/economy/sell-signal-runtime.ts` (1) + `sell-signal.ts` (1) + `session-price-snapshot.ts:83` + `commerce-listings-capture.ts:38` + `equipment-salvage-economy.ts` + `container-*.ts` (×4) + `hold-intent.ts` (×2) + `reservation.ts` (×2) + `recommendation-envelope.ts` (×4) + `inventory-recommendation-envelope.ts` (×3) = 20 | `catch` → null/unavailable | parseo / validación | 83: http sí, agregado no | Valoración «no disponible» | OK (inerte) / leve (83) |
| 18 | `src/halloween/halloween-runtime.ts:*` | `catch (error)` → `span.failure` / `storeFailure` | — | SÍ salvo 220/299/359/373/440 (`storeFailure` y `seedOwnedItems` no registran el error; el span exterior cierra con `status`) | `store_unavailable` | OK / parcial |
| 8 | `src/halloween/halloween-price-alert-runtime.ts:*` | idem | — | SÍ salvo 160/209 (`fail(error)` sin registro) | idem | OK / parcial |
| 28 | `src/halloween/halloween-store.ts:*` | `attempt.failure(localDebugStorageFailureCode(error))` | IDB | PARCIAL (código + cuota) | — | OK-ish |
| 5 | `src/halloween/halloween-evidence-service.ts:60,108,129,141,152` | `catch` | — | 60 SÍ; 108/129 http sí | — | OK |
| 3 | `src/halloween/halloween-note-backfill.ts:66` +2, `halloween-price-alert.ts:154`, `halloween-unlocks.ts:25` | `catch` | vault.read / reloj / cliente | 66 relanza tipado (sin causa) | backfill «parcial» | leve |
| 13 | `src/sessions/pilot-metrics-recorder.ts:58-221,249` | `catch` → false/null | Cualquier fallo del store de métricas del piloto | NO (sin diagnóstico; `grep` = 0) | Ajustes: `settings.pilot.status.unavailable` | TRAGADERO (medio: son los datos del piloto de David) |
| 12 | `src/sessions/pilot-metrics-store.ts:*` | `catch` → false/null | IDB | NO | idem | TRAGADERO (medio) |
| 2 | `src/sessions/pilot-metrics-export.ts:79,250` | `catch` → `'unavailable'` | export al vault lanza | NO; `exportPilotMetrics` (`main.ts` l.1638) no está en `run()` | Ajustes «no disponible» | TRAGADERO |
| 4 | `src/ui/settings-tab.ts:870,879,892,907` | `catch` → «failed» | Botones de depuración | SÍ (`main.ts` l.1174/1187/1230 en `run(support)`) | «failed» | OK |
| 1 | `src/ui/settings-tab.ts:1142` | `catch` → `announce('error')` | `updateSettings` lanza | SÍ (`run(settings_save)` l.3025) | «error» | OK |
| 26 | `src/ui/settings-tab.ts:*` resto (940-988 `void profileFlight`, callbacks de modales) | `void` / callback | `getPilotProfile` rechaza | NO (`.then` sin catch → `unhandledrejection`) | — | leve |
| 1 | `src/main.ts:2978` | `catch` → `'next_refresh'` | `inventoryAdvisor.reclassify` lanza al guardar ajustes | SÍ (workflow span l.158) | «se aplicará en el próximo refresco» | OK |
| 1 | `src/main.ts:3068` | callback `renderViews` | — | — | — | OK |
| 4 | `src/main.ts:3632,3633` | `.catch(() => undefined)` | `createInventoryAdvisorCommandCallbacks` | no se usa desde `main.ts` (`grep` = solo la definición) | — | OK (muerto) |
| 4 | `src/main.ts:3755,3785,3817,3822` | `catch` / `.catch` | export JSONL / `fireAndForgetLocal` sin runner / `consumeRecorded` | 3817: solo cuando NO hay runner; 3822: la promesa interna ya registró | — | OK |
| 5 | `src/main.ts:1879,1940,2841/2862` | `void` (`startLiveObservation`, `pilotMetrics.sessionStarted`) | — | 1879 va por `fireAndForgetLocal`; 2841/2862 pilot | — | OK / leve |
| 1 | `src/main.ts:2635` | callback `onSettlementDue` → `performStopManualSession` | — | cubierto por fila 2681 | — | ver 2681 |
| 6 | `src/ui/companion-view.ts:963,1102,1115` (`catch` vacíos) + `769` (`.catch`) + `1545` | `catch` | pilot metrics; `classifyPilotRecovery`; `dismissPendingProposal` | 1545: SÍ (`run(detection_proposal dismiss)` l.1742); resto NO | 1545: `modal.dismissFailed`; resto nada | 1545 OK; resto leve |
| 40 | `src/ui/companion-view.ts:*` resto (`void this.actions.*`, listeners) | `void` | Rechazos de `stopManualSession` (578, 1124), `captureSessionFinalNow` (587), `retrySessionSummarySave` (640), `rotateToNewSession` (643), `runRecovery` (684), `checkConnection` (370, 734), `reviewPendingProposal` (967), `stopPendingSession` (970) | Cada uno cae en su fila de `main.ts`; el `void` sin `.catch` añade un `global_error unattributed_origin` cuando la cadena lanza | — | cubiertos arriba |
| 23 | `src/ui/inventory-advisor-view.ts:*` | `void` / callbacks | Botones del advisor → `ProductActionController.run` / `runInventorySyncAction` | cubiertos por `product-action-controller.ts:283` y `inventory-vault-sync-run-controller` | — | cubiertos arriba |
| 2 | `src/ui/inventory-advisor-item-view.ts:216` (+207 `void`) | `catch` vacío | catálogo de precios inalcanzable | `resolvePriceHistoryItemCatalog` (`main.ts` l.1376) ya registró | ids numéricos | OK |
| 1 | `src/ui/manual-session-start-modal.ts:73` | `catch` → `manual.invalidDetails` | validación del formulario | NO (validación local) | útil | OK |
| 6 | `src/ui/session-history-panel.ts:96` +3, `session-history-scrub-controller.ts` (×2) | `void controller.load()` | `loadSessionHistory` → `scan()` | NO (fila session-history) | «no disponible» | cubierto |
| 4 | `src/ui/price-history-panel-view.ts:51,90,270` +1 | `void interactions.onLoad/onEnable` | `priceHistory.loadSeries` lanza | SÍ (runtime span l.240) | «no disponible» | OK |
| 5 | `src/ui/halloween-personal-valuation-settings.ts:111,113,152` +2 | `void this.apply` / `catch` → `save_failed` | `updateSettings` lanza | SÍ (`run(settings_save)`) | «no se pudo guardar» | OK |
| 1 | `src/ui/vault-folder-suggest.ts:25` | `void onSelectFolder` | `updateSettings` | SÍ | — | OK |
| 3 | `src/ui/session-card.ts:*` | callbacks | — | — | — | OK |
| 1 | `src/ui/price-history-note-block-controller.ts:67` | `catch` → `no_seed unreachable` | `ports.ensure` lanza | PARCIAL (`price-seed-panel-service.ts:168` no registra `open`) | Bloque «inalcanzable» | leve |
| 27 | `src/core/local-debug-action-runner.ts` (14), `local-debug-logger.ts` (8), `local-debug-persistence.ts` (3), `local-debug-writer.ts` (2) | `catch` | El propio logger falla | N/A: no puede registrarse a sí mismo; `LocalDebugStatus.state='degraded'` + `errorCode` (contrato l.96-112) | Aviso «diagnóstico degradado» en la Companion (`openLocalDebugSettings`, `main.ts` l.1153) | OK |
| 12 | `src/sessions/session-note-renderer.ts:71,503,648,658`, `session-note-model.ts:100`, `session-state-machine.ts:56`, `session-item-type-capture.ts:35`, `mumble-v2-shadow-proposal.ts:44,127`, `loot-presentation.ts:124`, `live-session-loot.ts:150`, `session-start-capture.ts:142` = 12 | `catch` → invalid/null | Parseo / máquina de estados | 71 (`hash_unavailable`) y 100 (`invalid_input`) llegan a `note.status='invalid'` → fila `session-note-writer`; 56 (`invariant_violation`) → `apply()` lanza → fila 840; 142 relanza tipado | — | OK (inerte) / cubiertos |

## 2. Propuestas de tarea hija (una por TRAGADERO no cubierto por H15.1)

**T1 · `SessionCommandController` registra la causa antes de convertirla en `'failed'`** — `src/ui/session-command-controller.ts:61`. Añadir un puerto opcional `diagnostics?: LocalDebugActionPort` a `SessionCommandPorts`, y en el `.catch((error) => …)` emitir `event({component:'session', action:'command_execute', level:'error', phase:'failure', code:'unknown_failure', state:id, message:error})`. Test: `session-command-controller.test.ts` con un `prepare` que devuelve `() => Promise.reject(new Error('boom'))` y un puerto fake; asserta que el fake recibió `phase:'failure'` con `message` que contiene `boom` y que `run()` sigue resolviendo `'failed'`.

**T2 · `performRecoverSession`/`performDiscardRecoveredSession` conservan `result.message`** — `src/main.ts:3336, 3356`. Lanzar `new Error(result.message)` (o un `SessionRecoveryError` con `status`) en vez de `'Recovery failed.'`, y en `main.ts:2586/2591` devolver `{phase:'failure'…}`-outcome cuando `sessionDispatch` reporte `'failed'`. Test: `main-session-recovery-migration.test.ts` o nuevo `main-recover-failure-observability.test.ts`: `sessions.recover` stub → `{status:'failed', message:'The recovered authority could not be persisted safely.'}`; asserta un registro `session_recover failure` cuyo `message` contiene ese texto.

**T3 · Barra de acciones y paleta: `'failed'` devuelto = registro de fallo** — `src/ui/product-action-controller.ts:207-216, 283` y `src/ui/product-shell.ts:44,45,171`. Inyectar `diagnostics?: LocalDebugActionPort` en `ProductActionController` y emitir `command_execute failure state:id` cuando `outcome === 'failed'` o el `execute` lance. Test: `product-action-controller.test.ts` con `execute: () => Promise.resolve('failed')` y un puerto fake; asserta `phase:'failure'`.

**T4 · `reviewPendingProposalOutcome` liga y registra el error** — `src/main.ts:1709`. `catch (error)` → `this.localDebugActions?.event({component:'detection', action:'detection_proposal', state:'review', level:'error', phase:'failure', code:'unknown_failure', message:error})` antes del notice; o devolver un outcome `{phase:'failure', code, details:{reason}}` para que `run()` lo escriba. Test: stub `pendingProposals.acknowledge` que lanza; asserta registro `detection_proposal failure` con `message`.

**T5 · Escritura de la nota de sesión: `note.status`+`message` al log** — `src/sessions/session-note-writer.ts:56,63,80` y `src/main.ts:2773`. Opción mínima: en `persistCompletedSessionSummary` (main.ts l.2751-2789) envolver en `run({component:'session', action:'session_finish', state:'note_write'})` y devolver `{phase:'failure', code:'storage_failure', details:{status:note.status, message}}` cuando `status` no sea `written|unchanged`; en el writer, `catch (error)` y propagar `error.message` en el `message` devuelto. Test: `main-session-note-economy.test.ts`: stub `vault.create` que lanza `new Error('EPERM')`; asserta registro `session_finish failure` con `details.status==='unavailable'` y `message` que contiene `EPERM`.

**T6 · `inspectCompletedSessionSummary` y `SessionHistory` con puerto de diagnóstico** — `src/main.ts:2745,1982,1995,2035` y `src/sessions/session-history.ts:192-390`. Inyectar `diagnostics?: LocalDebugActionPort` en `SessionHistory`; en cada `catch` → `span.failure(error, 'storage_failure', status)` antes de devolver `'unavailable'|'conflict'`. Test: `session-history.test.ts` con `vault.read` que lanza; asserta evento `vault_read failure` con `message`.

**T7 · `AlertEmitter` devuelve motivos y el runner los registra** — `src/alerts/alert-emitter.ts:61,65` y `src/main.ts:2092-2095`. `startChannel` conserva `error.message` en `{id, ok:false, reason}`; `emit()` devuelve `failed: [{id, reason}]`; `dispatchAlert` envuelve con `run()` y devuelve `{phase: failed.length ? 'failure' : 'success', code:'unavailable', details:{failed}}`. Test: `main-alert-wiring.test.ts`: canal `ingame` habilitado sin addon; asserta registro `notification_emit failure details.failed[0].reason === 'No in-game addon is connected.'`.

**T8 · `ensureAlertIngameServer` no tira el error del puerto** — `src/main.ts:2270`. `.catch((error) => { this.localDebugActions?.event({component:'notification', action:'notification_emit', state:'ingame_server_start', level:'error', phase:'failure', code:'unavailable', message:error}); return null; })` y reflejar `alertIngameServerError` en la fila de ajustes. Test: `startAlertIngameServer` stub que rechaza con `Object.assign(new Error('listen EADDRINUSE'), {code:'EADDRINUSE'})`; asserta registro con `EADDRINUSE`.

**T9 · Sell signal: el `catch` vacío registra** — `src/main.ts:2106`. `catch (error) { this.localDebugActions?.event({component:'price_history', action:'price_history_compact', state:'sell_signal', level:'error', phase:'failure', code:'unknown_failure', message:error}); }`. Test: `main-sell-signal-wiring.test.ts` con `ensureSeed` que lanza; asserta el registro.

**T10 · Sync de inventario/cartera: el estado `error` lleva causa y llega al log** — `src/ui/inventory-vault-sync-run-controller.ts:140,146,161`, `src/ui/vault-sync-controller.ts:92,118`, `src/inventory/inventory-vault-sync.ts:458`, `src/wallet/wallet-vault-sync.ts:264`. Los `catch` ligan `error` y lo guardan en `state.cause` (message solo); `main.ts` l.1444/1450/1484/1513 devuelven outcome `{phase:'failure', code:'storage_failure', details:{reason, cause}}` cuando `current().status==='error'`. Test: `inventory-vault-sync-run-controller.test.ts` con `previewSync` que lanza `Error('vault locked')`; asserta `state.reason==='capture_unavailable'` y `cause` contiene `vault locked`; test de `main` que asserta el registro `inventory_sync failure`.

**T11 · `previewManagedAssets` bajo `run()`** — `src/main.ts:2386-2402`. Envolver `perform` en `run({component:'assets', action:'managed_assets_preview'})` y en el `catch (error)` relanzar tras fijar la vista, o devolver outcome de fallo. Test: `managedAssets.preview` stub que lanza; asserta registro `managed_assets_preview failure`.

**T12 · Familia stores: `attempt.failure` recibe `errorName`** — `src/core/local-debug-persistence.ts:107-125,143-157` y los 8 stores que llaman `attempt.failure()` pelado (`session-runtime-store.ts:155,180,202,218,257`, `coordination-store.ts:54,68,75-76,90,108-109`, `pending-proposal-store.ts:59,94,132`, `session-detection-quality-store.ts:90,105,144`, `persistent-catalog-cache.ts:51,85,124,139,184,218,232,256,296`, `managed-assets-pointer.ts:46,78,111`). Ampliar `LocalDebugPersistenceAttempt.failure(code, error?)` para que el sink añada `errorName: error.name` (nunca `message`: puede llevar rutas) y usar `localDebugStorageFailureCode(error)` en todos. Test: `local-debug-persistence.test.ts`: `failure('storage_failure', new DOMException('x','QuotaExceededError'))` → evento con `code:'quota_exceeded'` y `errorName:'QuotaExceededError'`; test rojo previo: sin el cambio, `errorName` es `undefined`.

**T13 · `runtimeReady === false` por arranque ROTO se distingue de «arrancando»** — `src/main.ts:464 (fireAndForget initializeRuntime)` y `notifyRuntimeStarting` (l.2065). Guardar `this.runtimeFailure = error` en un `.catch` propio del `initializeRuntime` (la traza ya existe en el log) y que `notifyRuntimeStarting` emita `notices.pluginStartFailed` con `openLocalDebugSettings` cuando `runtimeFailure !== null`. Test: `main-deferred-runtime-startup.test.ts`: `initializeRuntime` stub que lanza; tras `onLayoutReady`, un `checkConnection()` emite el notice de arranque roto, no el de «arrancando».

**T14 · `http.ts:287` conserva el mensaje original de Electron** — `src/core/http.ts:283-289`. `new HttpTransportError('network', null, null, error instanceof Error ? error.message : 'Network request failed.')` (el sanitizador de rutas ya actúa en `message`). Test: `http.test.ts`: `request` que rechaza con `Error('net::ERR_NAME_NOT_RESOLVED')`; asserta que el registro `http_request failure` contiene `ERR_NAME_NOT_RESOLVED`.

**T15 · `connection_check` registra el estado final** — `src/main.ts:1098` y `src/account/connection-service.ts:71`. `perform` devuelve outcome `{phase: state.status==='error' ? 'failure' : 'success', code, state: state.reason, details}` (el `ConnectionState` no es `isOutcome`, así que hoy siempre `success ok`); en `failedState`, `catch (error)` conserva `error.message` para el registro. Test: `main.test.ts`: `gateway.checkConnection` que lanza `Error('schema')`; asserta `connection_check failure`.

**T16 · Pilot metrics: `PilotMetricsRecorder` con puerto de diagnóstico** — `src/sessions/pilot-metrics-recorder.ts:58-221` y `pilot-metrics-store.ts`. Inyectar `diagnostics?` y `span.failure(error,'storage_failure')` en cada `catch` antes del `false`; `exportPilotMetrics` (`main.ts:1638`) bajo `run()`. Test: `pilot-metrics-recorder.test.ts` con store que lanza; asserta evento `failure`.

**T17 · `manual-session-start-service.ts:806` (price capture) y `:1057/1084` (heartbeat) registran** — usar `this.diagnostics.event(...)` ya disponible (l.226). Test: `manual-session-start-service.test.ts`: `priceCapture.capture` que lanza; asserta `session_finish failure state:'price_capture'` y que el stop sigue devolviendo `stopped`.

## 3. Cifras al pie

Sumadas sobre la columna «n» de la tabla (141 filas) con: `node -e` que filtra `^\| \d+ \|`, suma `n`, y clasifica por la columna veredicto (`TRAGADERO` / `OK` / `cubiert|ver` / `leve`) y por la marca «fuera del censo» en la columna fichero.

- Fronteras revisadas: **718** = 675 del censo en alcance (693 − 18 de `src/platform/`; la suma de «n» en filas del censo cuadra con 675) + **43 fuera del censo** (ramas `'unexpected'`/`'unavailable'`/`failStop`/`failWithoutLease`/`runtimeReady` y los dos métodos públicos sin `run()`).
- **TRAGADERO: 241** (226 del censo + 15 fuera del censo). De ellas **43 graves** (las 19 filas en negrita: sin ninguna línea con causa en el log y mensaje genérico o ninguno).
- «leve» (causa perdida sin impacto de producto visible): 43.
- **OK: 304** (llega causa al log, o la frontera es inerte: validadores puros, callbacks de registro, diagnóstico fail-open).
- Cubiertas por otra fila (mismo punto de fallo aguas arriba): 130.
- Ya en curso H15.1 (`mapFailure`/`mapStopFailure`, `Start failed.`/`Stop failed.`, `pendingStartFailed`): 6 filas / 11 fronteras (incluidas en las 241). **No cubre** `SessionCommandController` (raíz del «0 líneas»), recover/discard/clear, la paleta/barra de acciones ni la nota de sesión.
